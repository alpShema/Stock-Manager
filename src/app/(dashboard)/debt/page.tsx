export default function DebtPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">Debt</h1>
      {/* TODO: status filter (PENDING / PARTIALLY_PAID / PAID), debt table, pay modal */}
    </div>
  );
}
